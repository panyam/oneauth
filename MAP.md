# Map

Auto-generated by `/design-map`. Do not edit by hand — rerun the skill instead.
Last generated: 2026-05-26T08:12:30Z against HEAD 0f92ff8.
Languages: go (21 folders).

## Suggested reading order

Roots first; folders that depend on others appear after their dependencies. Each folder name links to that folder's DESIGN.md.

1. [`accounts/`](accounts/DESIGN.md) — Owns the federated end-user account model (User, Identity, Channel) and store interfaces shared by local and provider-mediated auth flows — explicitly excludes HTTP handlers and OAuth token shapes.
2. [`core/`](core/DESIGN.md) — Transport-level foundation types — tokens, scopes, RFC 9396 authorization details, blacklist/rate-limit primitives, and a tiny user-id context shim — shared across every OneAuth subpackage.
3. [`grpc/`](grpc/DESIGN.md) — Authentication context utilities and server interceptors that propagate the authenticated user ID across the HTTP-to-gRPC boundary via metadata, with optional auth enforcement and switch-user impersonation.
4. [`httpauth/`](httpauth/DESIGN.md) — HTTP-layer plumbing for session-based auth — a session/JWT-aware login mux, a pluggable user-resolution middleware, and standalone CSRF / body-limit / security-header middlewares.
5. [`oauth2/`](oauth2/DESIGN.md) — Self-contained OAuth2 social-login providers (Google, GitHub) with a shared base, PKCE (RFC 7636), and state-cookie CSRF protection that deliver the authenticated user to a host-supplied callback.
6. [`saml/`](saml/DESIGN.md) — POC SAML 2.0 Service Provider adapter that mounts SP-initiated SSO/SLO routes on a gorilla/mux router and bridges the SAML assertion into oneauth's OAuth-shaped HandleUserFunc callback.
7. [`utils/`](utils/DESIGN.md) — Stateless crypto and encoding helpers — RSA/ECDSA key generation, PEM/PKCS8 codec, JWK/JWKS conversion, RFC 7638 kid thumbprints, JWT signing-method lookup, and Flask session-cookie decoding.
8. [`client/`](client/DESIGN.md) — Client-side OAuth SDK — discovers authorization servers, drives browser/CLI/M2M login flows, caches and refreshes tokens, and injects them into HTTP requests.
9. [`federatedauth/`](federatedauth/DESIGN.md) — Orchestrates provider-mediated (OAuth/SAML) authentication callbacks — turning provider userInfo into an accounts.User, linking new channels onto existing users, and establishing the session via httpauth.
10. [`keys/`](keys/DESIGN.md) — Signing-key storage, kid-indexed lookup, encryption-at-rest, grace-period rotation, and JWKS publication/consumption for OneAuth.
11. [`localauth/`](localauth/DESIGN.md) — Local username/password authentication — HTTP handlers and store-backed callbacks for signup, login, email verification, password reset, and credential linking onto OAuth-only users.
12. [`admin/`](admin/DESIGN.md) — OneAuth's client-administration surface — RFC 7591 Dynamic Client Registration, RFC 7592 self-service registration management, the proprietary /apps registry, admin-side CRUD, signing-key rotation with grace periods, and resource-token minting.
13. [`client/stores/fs/`](client/stores/fs/DESIGN.md) — Filesystem-backed CredentialStore that persists per-server ServerCredential entries in a single JSON file with owner-only permissions.
14. [`keystoretest/`](keystoretest/DESIGN.md) — Shared conformance test suite that every `keys.KeyStorage` backend (inmem, fs, gorm, gae) runs against to prove uniform put/get/delete/list, kid resolution, and asymmetric key round-trip semantics.
15. [`kidstoretest/`](kidstoretest/DESIGN.md) — Shared conformance test suite that every `keys.KidStorage` backend (in-memory KidStore, FS, GORM, GAE) drives via a per-test factory to prove uniform contract compliance.
16. [`appstoretest/`](appstoretest/DESIGN.md) — Shared contract test suite that every `admin.AppRegistrationStore` backend (inmem, fs, gorm) runs against to prove behavioral parity.
17. [`stores/fs/`](stores/fs/DESIGN.md) — File-backed implementations of every oneauth store interface, optimized for dev and single-node deployments via file-per-record JSON layouts with atomic temp-file + rename writes.
18. [`stores/gae/`](stores/gae/DESIGN.md) — Google Cloud Datastore-backed implementations of oneauth's account/identity/channel/token/key/username stores, with namespacing, JSON-blob payloads, and transactional rotation/revocation flows.
19. [`apiauth/`](apiauth/DESIGN.md) — API token authentication — token issuance (password / refresh / client_credentials / jwt-bearer / token-exchange), JWT validation middleware, RFC 7662 introspection, RFC 7009 revocation, RFC 8414 / OIDC discovery, and a transport-independent `OneAuth` core that composes focused interfaces (`TokenIssuer` / `TokenValidator` / `TokenIntrospector` / `TokenRevoker` / `ClientAuthenticator`).
20. [`stores/gorm/`](stores/gorm/DESIGN.md) — GORM/SQL-backed implementations of every oneauth store interface (users, identities, channels, verification tokens, refresh tokens, API keys, usernames, signing keys, kid keys, app registrations) plus a single AutoMigrate, portable across SQLite/MySQL/Postgres.
21. [`testutil/`](testutil/DESIGN.md) — Shared test setup helpers for oneauth — an in-process RS256 OAuth AS plus OIDC discovery, token-grant, JWKS, and JWT introspection fixtures used across package test suites.

## All packages

| Folder | Lang | Purpose |
|---|---|---|
| [`accounts/`](accounts/DESIGN.md) | go | Owns the federated end-user account model (User, Identity, Channel) and store interfaces shared by local and provider-mediated auth flows — explicitly excludes HTTP handlers and OAuth token shapes. |
| [`admin/`](admin/DESIGN.md) | go | OneAuth's client-administration surface — RFC 7591 Dynamic Client Registration, RFC 7592 self-service registration management, the proprietary /apps registry, admin-side CRUD, signing-key rotation with grace periods, and resource-token minting. |
| [`apiauth/`](apiauth/DESIGN.md) | go | API token authentication — token issuance (password / refresh / client_credentials / jwt-bearer / token-exchange), JWT validation middleware, RFC 7662 introspection, RFC 7009 revocation, RFC 8414 / OIDC discovery, and a transport-independent `OneAuth` core that composes focused interfaces (`TokenIssuer` / `TokenValidator` / `TokenIntrospector` / `TokenRevoker` / `ClientAuthenticator`). |
| [`appstoretest/`](appstoretest/DESIGN.md) | go | Shared contract test suite that every `admin.AppRegistrationStore` backend (inmem, fs, gorm) runs against to prove behavioral parity. |
| [`client/`](client/DESIGN.md) | go | Client-side OAuth SDK — discovers authorization servers, drives browser/CLI/M2M login flows, caches and refreshes tokens, and injects them into HTTP requests. |
| [`client/stores/fs/`](client/stores/fs/DESIGN.md) | go | Filesystem-backed CredentialStore that persists per-server ServerCredential entries in a single JSON file with owner-only permissions. |
| [`core/`](core/DESIGN.md) | go | Transport-level foundation types — tokens, scopes, RFC 9396 authorization details, blacklist/rate-limit primitives, and a tiny user-id context shim — shared across every OneAuth subpackage. |
| [`federatedauth/`](federatedauth/DESIGN.md) | go | Orchestrates provider-mediated (OAuth/SAML) authentication callbacks — turning provider userInfo into an accounts.User, linking new channels onto existing users, and establishing the session via httpauth. |
| [`grpc/`](grpc/DESIGN.md) | go | Authentication context utilities and server interceptors that propagate the authenticated user ID across the HTTP-to-gRPC boundary via metadata, with optional auth enforcement and switch-user impersonation. |
| [`httpauth/`](httpauth/DESIGN.md) | go | HTTP-layer plumbing for session-based auth — a session/JWT-aware login mux, a pluggable user-resolution middleware, and standalone CSRF / body-limit / security-header middlewares. |
| [`keys/`](keys/DESIGN.md) | go | Signing-key storage, kid-indexed lookup, encryption-at-rest, grace-period rotation, and JWKS publication/consumption for OneAuth. |
| [`keystoretest/`](keystoretest/DESIGN.md) | go | Shared conformance test suite that every `keys.KeyStorage` backend (inmem, fs, gorm, gae) runs against to prove uniform put/get/delete/list, kid resolution, and asymmetric key round-trip semantics. |
| [`kidstoretest/`](kidstoretest/DESIGN.md) | go | Shared conformance test suite that every `keys.KidStorage` backend (in-memory KidStore, FS, GORM, GAE) drives via a per-test factory to prove uniform contract compliance. |
| [`localauth/`](localauth/DESIGN.md) | go | Local username/password authentication — HTTP handlers and store-backed callbacks for signup, login, email verification, password reset, and credential linking onto OAuth-only users. |
| [`oauth2/`](oauth2/DESIGN.md) | go | Self-contained OAuth2 social-login providers (Google, GitHub) with a shared base, PKCE (RFC 7636), and state-cookie CSRF protection that deliver the authenticated user to a host-supplied callback. |
| [`saml/`](saml/DESIGN.md) | go | POC SAML 2.0 Service Provider adapter that mounts SP-initiated SSO/SLO routes on a gorilla/mux router and bridges the SAML assertion into oneauth's OAuth-shaped HandleUserFunc callback. |
| [`stores/fs/`](stores/fs/DESIGN.md) | go | File-backed implementations of every oneauth store interface, optimized for dev and single-node deployments via file-per-record JSON layouts with atomic temp-file + rename writes. |
| [`stores/gae/`](stores/gae/DESIGN.md) | go | Google Cloud Datastore-backed implementations of oneauth's account/identity/channel/token/key/username stores, with namespacing, JSON-blob payloads, and transactional rotation/revocation flows. |
| [`stores/gorm/`](stores/gorm/DESIGN.md) | go | GORM/SQL-backed implementations of every oneauth store interface (users, identities, channels, verification tokens, refresh tokens, API keys, usernames, signing keys, kid keys, app registrations) plus a single AutoMigrate, portable across SQLite/MySQL/Postgres. |
| [`testutil/`](testutil/DESIGN.md) | go | Shared test setup helpers for oneauth — an in-process RS256 OAuth AS plus OIDC discovery, token-grant, JWKS, and JWT introspection fixtures used across package test suites. |
| [`utils/`](utils/DESIGN.md) | go | Stateless crypto and encoding helpers — RSA/ECDSA key generation, PEM/PKCS8 codec, JWK/JWKS conversion, RFC 7638 kid thumbprints, JWT signing-method lookup, and Flask session-cookie decoding. |

## Dependency graph

```
accounts/          (no internal deps)
core/              (no internal deps)
grpc/              (no internal deps)
httpauth/          (no internal deps)
oauth2/            (no internal deps)
saml/              (no internal deps)
utils/             (no internal deps)
client/            → core/, utils/
federatedauth/     → accounts/, httpauth/
keys/              → utils/
localauth/         → accounts/, core/
admin/             → core/, keys/, utils/
client/stores/fs/  → client/
keystoretest/      → keys/, utils/
kidstoretest/      → keys/, utils/
appstoretest/      → admin/
stores/fs/         → accounts/, admin/, core/, keys/, localauth/, utils/
stores/gae/        → accounts/, core/, keys/, keystoretest/, kidstoretest/, localauth/, utils/
apiauth/           → accounts/, admin/, core/, keys/, localauth/, stores/fs/, utils/
stores/gorm/       → accounts/, admin/, appstoretest/, core/, keys/, keystoretest/, kidstoretest/, localauth/, utils/
testutil/          → admin/, apiauth/, core/, httpauth/, keys/, utils/
```
